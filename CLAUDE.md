# mdcli

CLI tool that renders Markdown in the terminal.

## Commands

- `cargo build` — build
- `cargo test` — run all tests
- `cargo test <name>` — run a single test
- `cargo clippy` — lint
- `cargo fmt` — format

## Development

SPEC-driven development using spec-kit + TDD. Specs live in `.specify/`.
